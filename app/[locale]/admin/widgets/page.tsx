// Gestion widgets (suggestions pending / planned / done)
// Données Notion → ZARA (lib/notion.ts)
export default function AdminWidgetsPage() {
  return (
    <section className="flex items-center justify-center py-32">
      <p className="font-display text-4xl text-muted">Admin — Widgets — placeholder</p>
    </section>
  )
}
